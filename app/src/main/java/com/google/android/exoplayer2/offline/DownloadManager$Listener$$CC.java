package com.google.android.exoplayer2.offline;

import com.google.android.exoplayer2.offline.DownloadManager;
import com.google.android.exoplayer2.scheduler.Requirements;

public abstract /* synthetic */ class DownloadManager$Listener$$CC {
    public static void onInitialized$$dflt$$(DownloadManager.Listener listener, DownloadManager downloadManager) {
    }

    public static void onDownloadChanged$$dflt$$(DownloadManager.Listener listener, DownloadManager downloadManager, Download download) {
    }

    public static void onDownloadRemoved$$dflt$$(DownloadManager.Listener listener, DownloadManager downloadManager, Download download) {
    }

    public static void onIdle$$dflt$$(DownloadManager.Listener listener, DownloadManager downloadManager) {
    }

    public static void onRequirementsStateChanged$$dflt$$(DownloadManager.Listener listener, DownloadManager downloadManager, Requirements requirements, int notMetRequirements) {
    }
}
