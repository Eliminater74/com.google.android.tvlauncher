package com.google.android.exoplayer2.audio;

public abstract /* synthetic */ class AudioListener$$CC {
    public static void onAudioSessionId$$dflt$$(AudioListener audioListener, int audioSessionId) {
    }

    public static void onAudioAttributesChanged$$dflt$$(AudioListener audioListener, AudioAttributes audioAttributes) {
    }

    public static void onVolumeChanged$$dflt$$(AudioListener audioListener, float volume) {
    }
}
